# Переменные окружения для режима разработки
# Используется при запуске: npm run dev

# URL бэкенда API
# Если не указан, используется Vite прокси: /api -> http://localhost:80
# Раскомментируйте и укажите свой URL бэкенда, если он отличается от localhost:80
# VITE_API_BASE_URL=http://localhost:80
# VITE_API_BASE_URL=http://192.168.1.8:80
